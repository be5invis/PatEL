#!/usr/bin/env node
var patel = require('../index');
var fs = require('fs');
var input = fs.readFileSync(process.argv[2], 'utf-8');
var gs = patel.globals();
process.stdout.write(patel.compile(patel.parse(input), gs));